<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- 首页按钮字体样式 -->
    <style name="HomeBtnTv">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="LoginLayoutStyle">
        <item name="android:paddingLeft">@dimen/space_66</item>
        <item name="android:paddingRight">@dimen/space_66</item>
    </style>

    <style name="LoginLayoutStyle2">
        <item name="android:layout_marginLeft">@dimen/space_66</item>
        <item name="android:layout_marginRight">@dimen/space_66</item>
    </style>

    <style name="LoginViewStyle">
        <item name="android:layout_width">250dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="dialog_custom" parent="@android:style/Theme.Panel">
        <item name="android:windowBackground">@color/half_transparent</item>
    </style>

    <style name="dialog_loading" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
    </style>
</resources>
